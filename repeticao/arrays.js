/*let num = [5, 8, 4, 3, 10]
num[3] = 6 //adicionar elemento na posicao 3.
num.push(7) // adicionar o valor na ultima posicao do array.
num.length // saber o tamanho do array (contar) - atributo
num.sort() // ordenar em ordem crescente
console.log(`Nosso vetor é o ${num}`)*/

/*let valores = [8, 1, 7, 4, 2, 9]

for (let pos = 0; pos < valores.length; pos++){
    console.log(`A posição ${pos} tem o valor ${valores[pos]}`)

for(let pos in valores){
    console.log(`A posição ${pos} tem o valor ${valores[pos]}`)
}

let num = [5, 8, 2, 9, 3]
num.push(1)
num.sort()
console.log(num)
console.log(`O vetor tem ${num.length} posições`)
console.log(`O primeiro vetor é ${num[0]}`)
let pos = num.indexOf(8)
if(pos == -1){
    console.log('O valor não foi encontrado!')
} else {
console.log(`O valor 8 esta na posicao ${pos}`)
}*/